<div>
  <h5>Comment by <%= User.find_by(comment.user_id)[:username] %></h5>
  <pre class="comment"><%= comment.body %></pre>
  <p>Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later</p>
</div>
